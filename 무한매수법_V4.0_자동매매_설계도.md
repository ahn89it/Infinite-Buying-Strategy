# 무한매수법 V4.0 (일반모드/리버스모드) 자동매매 프로그램 설계도

> 이 문서는 클로드 코드(Claude Code)에게 전달하여 파이썬 자동매매 모듈을 구현시키기 위한 설계 명세서입니다.
> 방법론 원저작권은 라오어(네이버 카페 "무한매수법 & 밸류리밸런싱 공부모임")에 있으며, 본 문서는 개인 학습·자동화 목적의 구현 스펙입니다.

---

## 0. 전체 목표

- 대상 종목: TQQQ (필요 시 SOXL 확장 고려, 우선은 TQQQ 단일 종목으로 구현)
- **기존 Kiwoom 자동매매 프로젝트와는 완전히 독립된 새 프로젝트**로 구성 (코드 공유/의존 없음, Kiwoom OpenAPI만 동일하게 사용)
- 매일 장 마감 후(프리장 시작 시점) 상태를 갱신하고 다음 거래일 주문을 자동 생성/제출
- 상태(원금, T값, 평단, 보유수량, 모드)는 프로그램 재시작에도 유지되어야 함 (파일 또는 DB 영속화)

---

## 1. 상태(State) 모델

프로그램이 매일 추적해야 하는 핵심 상태값:

| 필드 | 설명 |
|---|---|
| `mode` | `"NORMAL"` (일반모드) 또는 `"REVERSE"` (리버스/소진모드) |
| `phase` | 일반모드일 때만 사용: `"FIRST"`(첫매수), `"FIRST_HALF"`(전반전), `"SECOND_HALF"`(후반전) |
| `split_count` | 분할수 (20 또는 40, 초기 설정 후 고정) |
| `principal` | 총 원금 |
| `remaining_cash` | 잔금 (원금 - 누적 매수금액 + 누적 매도금액) |
| `T` | 회차값 (소수점 허용, 예: 5.165479525) |
| `avg_price` | 현재 평단가 |
| `holding_qty` | 현재 보유 수량 |
| `cycle_start_date` | 현재 사이클 시작일 |
| `reverse_day_count` | 리버스모드 진입 후 경과일수 (D1, D2, ...) |
| `reverse_prev_qty` | 리버스모드 전일 보유수량 (매도수량 계산용) |

이 상태는 JSON 또는 SQLite로 매일 장 마감 후 저장하고, 프로그램 시작 시 항상 로드해서 이어받아야 합니다. **상태 파일이 없거나 손상된 경우 수동 개입 없이는 절대 임의값으로 시작하지 않도록** 안전장치(assert/예외 발생)를 둡니다.

---

## 2. T값 갱신 규칙 (일반모드 기준)

| 이벤트 | T값 변화 |
|---|---|
| 1회 매수 완료 (해당 회차 매수금 전액 체결) | `T += 1` |
| 절반 매수 체결 (전반전 매수의 절반 금액만 체결된 경우) | `T += 0.5` |
| 쿼터매도 발생 (보유수량의 1/4을 별지점에서 매도) | `T *= 0.75` |
| 지정가매도 체결 후 같은 날 LOC매수까지 체결 | `T = T * 0.25 + 1` (매수가 1회 완료분일 때) 또는 `T = T * 0.25 + 0.5` (매수가 절반분일 때) |

- 소수점 자릿수 제한 없음. `Decimal` 타입 사용 권장 (float 누적 오차 방지).
- 매일 장 마감 후, 그날 체결 내역을 순서대로 재생(replay)하여 T값을 갱신하는 방식으로 구현 (이벤트 소싱 패턴 권장).

---

## 3. 별지점(목표가) 공식

```
40분할 TQQQ:  별% = (15 - 0.75 * T) %
20분할 TQQQ:  별% = (15 - 1.5  * T) %

별지점 = 평단가 * (1 + 별% / 100)
매수점 = 별지점 - 0.01달러   (매도점과 겹침 방지)
매도점 = 별지점
```

- `T`가 `split_count / 2` 이하 → **전반전**
- `T`가 `split_count / 2` 초과 → **후반전** (별%가 음수로 전환, 평단보다 낮은 가격이 목표가가 됨)
- `T > split_count - 1` → **리버스모드 진입 트리거**

구현 시 `star_percent(T, split_count)` 함수로 분리하고, 단위 테스트로 20분할/40분할 각각 T=1~40 전 구간 검증.

---

## 4. 1회매수금 계산 (매일 변동)

```
20분할: 1회매수금 = remaining_cash / (20 - T)
40분할: 1회매수금 = remaining_cash / (40 - T)
```

- 매일 잔금과 T값에 따라 미세하게 변동하므로, **매수 주문 직전에 항상 재계산**해야 함 (전날 값을 캐싱해서 쓰면 안 됨).
- `T`가 분할수에 근접할수록 분모가 작아져 1회매수금이 커지는 특성을 감안해 0 또는 음수 분모 방어 코드 필요 (리버스모드 전환 조건과 연동해서 발생 전에 처리되어야 정상).

---

## 5. 매수 로직 (전부 LOC 주문)

### 5-1. 첫 매수 (T=0, 보유량 0)

- 전일 종가 대비 **10~15% 위** 가격에 "큰수" LOC 매수 주문 (증권사 ±20% 주문 제한 우회용, 실제 체결 목적 아님)
- 그 아래로 실제 체결을 노리는 단계별 LOC 매수 주문 추가
- 구현 메모: "큰수 매수"는 방법론이 아니라 실무적 우회 장치이므로, 별도 함수 `place_decoy_order()`로 분리해 문서화

### 5-2. 전반전 매수 (T = 1 ~ 분할수/2 미만)

- 1회매수액의 **절반** → 별지점 가격에 LOC 매수
- 1회매수액의 **절반** → 평단가 가격에 LOC 매수
- 추가 하락 대비 아래쪽에 보조 LOC 매수 주문 (단계별, 큰 하락 시 대응)

### 5-3. 후반전 매수 (T = 분할수/2 ~ 분할수-1)

- 1회매수액 **전액** → 별지점(평단보다 낮은 가격) LOC 매수
- 추가 하락 대비 아래쪽 보조 LOC 매수 주문
- `T > 분할수 - 1` 판정 시 → **리버스모드 진입**, 이후 매수 로직은 6번 섹션으로 전환

---

## 6. 매도 로직 (전반전/후반전 공통, 일반모드)

- 보유수량의 **1/4** → 별지점 LOC 매도 (쿼터매도)
- 나머지 **3/4** → 지정가 매도, TQQQ 기준 평단 **+15%**
- 주문 타이밍:
  - 지정가매도: 프리장 시작 시각(서머타임 17시 / 비서머타임 18시)에 걸어서 프리~본장~애프터까지 유지
  - LOC매수/매도: 본장 중 아무 때나 가능
  - **주간거래(데이마켓) 시간대 주문 절대 금지** — 실행 전 시간 체크 로직 필수

---

## 7. 리버스모드 (소진모드)

### 7-1. 진입 조건
- 일반모드에서 `T > 분할수 - 1`이 되면 그 다음 거래일부터 리버스모드 진입

### 7-2. 리버스모드 로직

```
별지점 = 직전 5거래일 종가 평균
매도수량 = 직전 보유수량을 등분 (20분할 → 10등분, 40분할 → 20등분, 내림)
```

- **리버스모드 첫날(D1)**: 계산된 수량을 MOC(장마감 시장가) 무조건 매도
- **D2 이후**: 그날그날 새로 계산한 매도수량을, 직전 5거래일 종가 평균 위쪽에서 LOC 매도로 걸기
- 매일 남은 보유수량 기준으로 등분 재계산 → 자연히 매도수량이 점점 줄어드는 구조

### 7-3. 종료(일반모드 복귀) 조건

```
종가 >= 평단가 * (1 - 0.15)   (TQQQ, 즉 평단대비 -15% 위로 올라오면)
```
- 조건 충족 다음 거래일부터 일반모드로 복귀 (T, 잔금 등은 리버스모드 종료 시점 기준으로 재설정 필요 — 이 부분은 실제 재시작 시 복리/단리 선택과 연계해서 사용자에게 확인받는 단계를 넣는 것을 권장)

---

## 8. 사이클 종료/재시작

- 보유수량이 0이 되면 사이클 종료 → 새 사이클 시작 (T=0, 보유량=0으로 초기화)
- 지정가매도 체결 후 같은 날 급락으로 LOC매수가 다시 체결되는 경우 → 사이클 종료 아님, 계속 진행 (T값은 2번 규칙 적용)
- 재시작 시 복리(이전 사이클 수익 포함 원금) / 단리(고정 원금) 선택 가능하도록 설정값으로 분리

---

## 9. 거래 이력 / 수익률 저장 (Trade History)

`event_log.py`(T값 재생용 내부 로그)와는 **별도**로, 사람이 조회·분석할 수 있는 거래 이력 저장 계층을 둡니다.

### 9-1. 저장 시점과 목적 구분

| 로그 종류 | 목적 | 파일/테이블 |
|---|---|---|
| 이벤트 로그 (`event_log.py`) | T값 재계산용 내부 replay | `event_log.jsonl` (append-only) |
| 거래 이력 (`trade_history.py`) | 사람이 보는 매수/매도/수익률 기록 | SQLite `trade_history` 테이블 (또는 CSV) |

### 9-2. 스키마

**매수 기록 (buy_records)**

| 필드 | 설명 |
|---|---|
| `id` | PK |
| `cycle_id` | 현재 사이클 번호 (사이클 종료마다 증가) |
| `buy_date` | 매수 체결일 (YYYY-MM-DD) |
| `buy_price` | 체결 단가 |
| `buy_qty` | 체결 수량 |
| `buy_amount` | 체결가 × 수량 |
| `order_type` | `"FIRST"` / `"HALF_STAR"` / `"HALF_AVG"` / `"FULL_STAR"` / `"REVERSE"` 등 매수 유형 |
| `T_after` | 이 매수 체결 직후의 T값 (state 스냅샷) |
| `avg_price_after` | 이 매수 체결 직후의 평단가 (state 스냅샷) |

**매도 기록 (sell_records)**

| 필드 | 설명 |
|---|---|
| `id` | PK |
| `cycle_id` | 사이클 번호 |
| `sell_date` | 매도 체결일 |
| `sell_price` | 체결 단가 |
| `sell_qty` | 체결 수량 |
| `sell_amount` | 체결가 × 수량 |
| `order_type` | `"QUARTER"`(쿼터매도) / `"LIMIT_15PCT"`(지정가매도) / `"REVERSE_MOC"` / `"REVERSE_LOC"` |
| `avg_price_at_sell` | 매도 시점 평단가 (수익률 계산 기준, state 스냅샷) |
| `return_pct` | **(sell_price - avg_price_at_sell) / avg_price_at_sell × 100** |
| `profit_amount` | **(sell_price - avg_price_at_sell) × sell_qty** — 이 매도 건의 실현 수익금 (원화/달러) |
| `T_after` | 매도 체결 직후 T값 (state 스냅샷) |

**사이클 요약 (cycle_summary)** — 사이클 종료(보유수량 0) 시점에 자동 생성

| 필드 | 설명 |
|---|---|
| `cycle_id` | PK |
| `start_date` | 사이클 시작일 |
| `end_date` | 사이클 종료일 |
| `total_buy_amount` | 사이클 내 총 매수금액 |
| `total_sell_amount` | 사이클 내 총 매도금액 |
| `cycle_profit_amount` | **total_sell_amount - total_buy_amount** (사이클 실현 수익금) |
| `cycle_return_pct` | (total_sell_amount - total_buy_amount) / total_buy_amount × 100 |
| `hit_reverse_mode` | 리버스모드 경유 여부 (bool) |
| `duration_days` | 사이클 소요 거래일수 |

### 9-3. 구현 원칙

- 매수/매도 주문이 **실제 체결 확인된 시점**에만 기록 (미체결 주문은 저장 안 함 — Kiwoom 체결 콜백에서 훅)
- 각 기록에는 그 순간의 `state` 스냅샷(T값, 평단, 잔금, 모드)을 함께 남겨서 나중에 특정 시점 상태를 그대로 재현/검증할 수 있게 함
- `return_pct`는 매도 건별로 계산 + 사이클 종료 시 `cycle_return_pct`로 한 번 더 집계 (건별 수익률과 사이클 전체 수익률이 다를 수 있음 — 쿼터매도/지정가매도가 서로 다른 시점의 평단 기준일 수 있기 때문)
- 조회 편의를 위해 `trade_history.py`에 `export_to_csv()` 함수 포함 (엑셀에서 바로 열람 가능하도록)
- 대시보드/리포트가 필요하면 이 테이블을 기반으로 별도 스크립트로 시각화 (설계도 범위 밖, 추후 확장)

### 9-4. 전체 누적 성과 (portfolio_summary) — 사이클을 넘어선 계좌 전체 관점

사이클 단위 수치만으로는 "이 전략을 시작한 뒤로 총 얼마를 벌었는지"를 바로 알 수 없으므로, 별도로 계좌/전략 전체를 누적 집계하는 테이블(또는 매일 갱신되는 단일 row)을 둡니다.

| 필드 | 설명 |
|---|---|
| `strategy_start_date` | 전략 시작일 (최초 원금 투입일) |
| `initial_principal` | 최초 투입 원금 |
| `total_realized_profit` | **지금까지 종료된 모든 사이클의 profit_amount 합계** (실현 수익금) |
| `total_realized_return_pct` | total_realized_profit / initial_principal × 100 (누적 실현 수익률) |
| `current_unrealized_pnl` | 현재 진행 중인 사이클의 평가손익 = (현재가 − 평단) × 보유수량 |
| `current_unrealized_return_pct` | (현재가 − 평단) / 평단 × 100 |
| `total_equity` | 현재 계좌 총평가금액 (현금 잔금 + 보유주식 평가금액) |
| `total_return_pct` | **(total_equity − initial_principal) / initial_principal × 100** (실현+미실현 합산 전체 수익률) |
| `completed_cycles` | 완료된 사이클 수 |
| `last_updated` | 마지막 갱신 시각 |

- 이 테이블은 매수/매도 체결이 있을 때마다, 그리고 매일 장 마감 후 시세 갱신 시점에 재계산합니다.
- `total_return_pct`가 사용자가 가장 궁금해할 "결국 얼마 벌었나"에 해당하는 최종 지표입니다.

---

## 10. 모듈 구조 제안

```
infinite_buying_v4/
├── state.py           # State 데이터클래스, 영속화(save/load)
├── formulas.py         # star_percent(), buy_amount(), avg_price 계산 등 순수함수
├── normal_mode.py       # 일반모드 매수/매도 주문 생성 로직
├── reverse_mode.py      # 리버스모드 매수/매도 주문 생성 로직
├── scheduler.py         # 장 시간대별 실행 스케줄 (프리장/본장/애프터 타이밍)
├── kiwoom_adapter.py     # Kiwoom OpenAPI 연동 인터페이스 (이번 프로젝트 안에서 새로 작성, 기존 프로젝트와 무관)
├── event_log.py         # 체결 이벤트 기록 및 T값 재생(replay) 처리
├── trade_history.py      # 매수일/매도일/수익금/수익률/state 스냅샷 저장 (9번 섹션)
├── dashboard/
│   ├── server.py         # 로컬 웹 대시보드 서버 (13번 섹션)
│   └── static/
│       └── index.html      # 보드 UI (단일 페이지)
└── tests/
    ├── test_formulas.py         # 별지점/1회매수금 T=1~40 전구간 검증
    ├── test_state_transition.py    # T값 갱신 케이스별 검증
    ├── test_mode_switch.py       # 일반↔리버스 전환 경계값 테스트
    └── test_trade_history.py      # 매수/매도 기록 및 수익금/수익률 계산 검증
```

---

## 11. 필수 방어 코드 / 예외 처리

- 상태 파일 로드 실패 시 무조건 예외 발생 (임의값으로 시작 금지)
- 체결 내역과 로컬 상태 불일치 감지 시 자동매매 중단 + 알림 (텔레그램/이메일 등, 이번 프로젝트 안에 신규 구현)
- 리버스모드 매도수량이 0으로 계산되는 경우(보유수량이 등분수보다 작을 때) 최소 1주 매도로 보정
- 장 운영시간(서머타임/비서머타임) 자동 판별 로직 (미국 서머타임 규칙 반영)
- 증권사 API 주문 실패/재시도 로직 (`kiwoom_adapter.py` 내부에 신규 구현)

---

## 12. 한눈에 보는 대시보드 (Board)

상세 로그(9번)와는 별개로, **현재 상태를 한 화면에서 바로 파악**할 수 있는 보드를 둡니다. 기존 인프라(로컬 PC에서 자동매매 실행 중)를 감안해 별도 서버 구축 없이 가장 가볍게 갈 수 있는 방식으로 설계합니다.

### 12-1. 구현 방식 제안

- 파이썬 내장 `http.server` 또는 경량 프레임워크(FastAPI/Flask 중 택1)로 로컬 웹서버 실행 → 브라우저에서 `localhost:포트`로 접속
- 데이터 소스는 9번의 SQLite `trade_history` + `portfolio_summary` 테이블을 그대로 읽기만 함 (별도 계산 로직 중복 금지)
- 매수/매도 체결 시, 또는 최소 몇 분 간격으로 자동 갱신 (폴링 또는 파일 변경 감지)
- 나중에 스마트폰에서도 보고 싶다면 같은 서버를 집 공유기 내부망에 열어두는 정도로 충분 (외부 노출/인증은 범위 밖)

### 12-2. 보드에 표시할 항목

**상단 요약 카드 (portfolio_summary 기반)**
- 총평가금액 (`total_equity`)
- 누적 총수익금 / 총수익률 (`total_equity - initial_principal`, `total_return_pct`)
- 실현 수익금 / 실현 수익률 vs 평가(미실현) 손익 — 구분해서 병기
- 현재 모드 배지: 🟢 일반모드(전반전/후반전) / 🔴 리버스모드

**현재 사이클 현황**
- 사이클 시작일, 경과일수
- 현재 T값 / 분할수 (예: "T=14 / 40")
- 현재 평단가, 현재가, 보유수량
- 다음 매수 목표가(별지점), 다음 매도 목표가

**최근 거래 내역 테이블 (최근 10~20건)**
- 날짜 | 매수/매도 | 가격 | 수량 | 유형 | 수익금 | 수익률

**사이클별 성과 히스토리 (차트)**
- 사이클별 `cycle_return_pct` 막대그래프 또는 누적 자산 곡선 (`total_equity` 시계열)

### 12-3. 우선순위

- 1차: 표+숫자만으로 된 심플한 대시보드 (요약 카드 + 최근 거래 테이블)
- 2차(선택): 누적 자산 곡선 차트 추가 (Chart.js 등 프론트엔드 라이브러리 활용)
- 처음부터 화려하게 만들기보다, `portfolio_summary`/`trade_history` 스키마가 안정된 뒤 마지막 단계에 붙이는 것을 권장 (스키마가 바뀌면 대시보드도 다시 손봐야 하므로)

---

## 13. 클로드 코드에게 줄 때 함께 전달할 것

1. 이 설계도 파일
2. Kiwoom OpenAPI 사용 환경: 32bit Python 여부, 실투자/모의투자 구분 (구체적인 TR명/함수명은 클로드 코드가 Kiwoom OpenAPI 공식 문서를 참고해 직접 조사·구현하도록 맡기면 됩니다 — Agh님이 미리 알아둘 필요 없음)
3. 초기 설정값: 원금, 분할수(20/40), 종목(TQQQ), 시작 T값/평단(신규 시작이면 0/없음)

**진행 순서 제안**: `formulas.py` → `state.py` → `trade_history.py` → `normal_mode.py` → 단위테스트 → `reverse_mode.py` → 단위테스트 → `scheduler.py` + `kiwoom_adapter.py` 통합 → (스키마 안정화 후) `dashboard/` 순으로 모듈 단위로 나눠서 클로드 코드 세션을 진행하면 토큰 소모와 디버깅 범위를 모두 줄일 수 있습니다.
