# 무한매수법 V4.0 신규 노트북 설정 가이드

## 문서 메타

| 항목 | 내용 |
|---|---|
| 버전 | 1.0 |
| 작성일 | 2026-09-01 |
| 목적 | 개발/검증에 쓰던 노트북이 아닌, **실제 자동매매를 상시 실행할 노트북**에 이 프로젝트를 새로 설정하는 절차 |
| 전제 | 개발 노트북에서는 아직 `bootstrap.py`를 실행한 적이 없어(=실제 매매 상태가 존재하지 않아) 새 노트북에서 **완전히 새로 시작**하는 것을 기준으로 작성했습니다. 만약 개발 노트북에 이미 진행 중인 사이클이 있다면 6장을 먼저 읽으세요 |
| 관련 문서 | `무한매수법_V4.0_구현설명서.md`(모듈 설명), `무한매수법_V4.0_운영_런북.md`(장애 대응·설정 전수표) |

---

## 1. 준비물 체크리스트

- [ ] GitHub 저장소 접근 권한 (`git clone`이 되는지 확인)
- [ ] 키움증권 **실투자용** App Key / App Secret (계좌에 귀속된 값 — 노트북이 바뀌어도 동일하게 재사용 가능, 재발급 불필요)
- [ ] Docker Desktop (권장 경로) 또는 Python 3.13 이상 (로컬 실행 경로)
- [ ] 원금(`PRINCIPAL`), 분할수(`SPLIT_COUNT`) 등 실제로 쓸 설정값

---

## 2. 코드 받기

```bash
git clone https://github.com/ahn89it/Infinite-Buying-Strategy.git
cd Infinite-Buying-Strategy
```

최신 커밋(`4bc833c` 이상)까지 받아졌는지 확인하세요:

```bash
git log --oneline -1
```

> **왜 최신 커밋이 중요한가**: 2026-09-01에 `config.py`의 실제 인증 버그를 수정했습니다
> (공식 `kwcli` 패키지가 자체적으로 읽는 `KIWOOM_MODE` 환경변수가 없으면 모든 API 호출이
> `ModeNotConfiguredError`로 실패하던 문제). 이 수정 이전 커밋으로 받으면 새 노트북에서
> 주문 제출/시세 조회가 전부 실패합니다. 자세한 내용은 운영 런북 변경이력(2026-08-03 4차) 참고.

---

## 3. `.env` 준비 — 이 파일은 git에 없습니다

`.env`는 `.gitignore`에 걸려 있어 `git clone`으로는 절대 따라오지 않습니다(App Secret 등
민감정보가 있기 때문에 의도된 동작입니다). 새 노트북에서 직접 만들어야 합니다.

```bash
cp .env.example .env      # macOS/Linux
Copy-Item .env.example .env   # Windows PowerShell
```

### 3-1. App Key/Secret 옮기기

App Key/Secret은 **키움 계좌에 귀속된 값**이라 노트북이 달라져도 그대로 재사용할 수
있습니다(다시 발급받을 필요 없음). 개발 노트북의 `.env`에 있는 값을 새 노트북으로
옮길 때는 다음 중 안전한 방법을 쓰세요:

- USB 등 물리 매체로 직접 옮기기
- 비밀번호 관리자(1Password, Bitwarden 등)에 저장해두고 새 노트북에서 조회
- 키움 OpenAPI 포털에서 직접 다시 확인해서 새로 타이핑

**절대 하면 안 되는 것**: 이메일, 메신저, 공개 저장소 등 평문으로 전송/저장하는 것.

### 3-2. `.env`에 채워야 할 값

`.env.example`의 각 항목을 채우세요(전체 변수 설명은 운영 런북 2장 "설정 전체 표" 참고):

| 변수 | 설정 예 | 비고 |
|---|---|---|
| `APP_KEY` / `APP_SECRET` | 키움 실투자용 값 | 필수 |
| `DRY_RUN` | `true` | **반드시 true로 시작하세요.** 새 환경(다른 네트워크, 다른 Python/Docker 설치)에서 처음 도는 것이므로, 실주문 전에 로그부터 확인하는 것이 안전합니다 |
| `TICKER` / `EXCHANGE_CODE` | `TQQQ` / `ND` | 기본값 그대로면 비워둬도 됨 |
| `SPLIT_COUNT` | `20` 또는 `40` | |
| `PRINCIPAL` | 실제 투입 원금(USD) | 필수 |
| `COMPOUND_ON_RESTART` | `true`/`false` | 복리/단리 |

---

## 4. 실행 환경 준비

### 4-1. Docker (권장)

```bash
docker compose build
```

### 4-2. 로컬 Python (Docker를 쓰지 않는 경우)

```bash
# Python 3.13 이상 필요 (kwcli 패키지 요구사항)
python --version
pip install -r requirements.txt
```

두 경로 모두, `zoneinfo`가 쓰는 시간대 데이터(`tzdata`)는 `kwcli`의 의존성인 `pandas`가
자동으로 함께 설치하므로 별도 설치가 필요 없습니다(Windows 포함).

---

## 5. 신규 시작 (딱 1회만 실행)

```bash
# Docker
docker compose run --rm infinite_buying python -m infinite_buying_v4.bootstrap

# 로컬 Python
python -m infinite_buying_v4.bootstrap
```

T=0, 보유 0, 사이클 1번 상태가 새로 생성됩니다. **이미 상태가 있으면 예외를 던지고
거부**하도록 되어 있으니, 실수로 두 번 실행해도 기존 데이터가 덮어써지지 않습니다.

---

## 6. (해당하는 경우만) 개발 노트북에 이미 진행 중인 사이클이 있다면

이 경우 5장의 `bootstrap.py`를 새 노트북에서 실행하면 **안 됩니다** — 새 사이클로
초기화되어 기존 진행 상황(T값, 평단가, 보유수량)을 잃어버립니다. 대신:

1. 개발 노트북에서 자동매매 프로세스(`scheduler.py`)를 완전히 중지합니다.
2. 개발 노트북의 `data/` 디렉터리 전체(`infinite_buying.db`, `event_log.jsonl`)를 새
   노트북의 같은 경로로 복사합니다.
3. 새 노트북에서는 `bootstrap.py`를 실행하지 않고, 바로 7장(기동)으로 넘어갑니다.
4. `python -m infinite_buying_v4.verify_replay`로 옮긴 상태의 T값이 이벤트 로그
   재생 결과와 일치하는지 확인합니다(운영 런북 8-3장).

---

## 7. 기동

```bash
docker compose up -d
docker compose logs -f infinite_buying   # 자동매매 로그
docker compose logs -f dashboard          # 대시보드 로그
```

대시보드 접속: `http://<새 노트북의 IP 또는 localhost>:8000` (같은 공유기 내부망의
다른 기기에서도 접속 가능, 외부 인터넷 노출은 범위 밖 — 구현설명서 1-12절 참고).

`DRY_RUN=true` 상태로 최소 며칠간 로그를 확인해 정상 동작을 검증한 뒤,
`.env`의 `DRY_RUN`을 `false`로 바꾸고 컨테이너를 재시작하면 실주문이 시작됩니다.

```bash
docker compose restart infinite_buying
```

---

## 8. ⚠️ 가장 중요한 주의사항: 두 노트북을 동시에 켜두지 마세요

개발 노트북과 새 자동매매 노트북에서 **`scheduler.py`(또는 `docker compose up`으로 뜨는
`infinite_buying` 서비스)를 동시에 실행하면 안 됩니다.**

중복 실행 방지(`daily_run_log`)는 **노트북별 로컬 SQLite 파일 안에서만** 작동하는
장치입니다. 서로 다른 노트북·서로 다른 DB 파일이면 이 잠금이 서로를 인지하지 못하고
각자 "오늘 처음 실행하는 것"이라고 착각해, **같은 실제 계좌에 주문이 두 번(또는 그 이상)
나갈 수 있습니다.** 자동매매는 항상 노트북 한 대에서만 켜두세요. (운영 런북 6장
"중복 실행/멱등성" 참고 — 거기 설명된 멱등성은 프로세스 재시작에 대한 것이지, 서로 다른
머신 간 동시 실행을 막아주지 않습니다.)

새 노트북으로 완전히 이전하는 경우, 개발 노트북 쪽에서는:
- `docker compose down` (또는 `scheduler.py` 프로세스 종료)
- 자동 시작 설정(있다면 부팅 시 자동 실행 등)을 해제

---

## 9. 이전 후 확인 체크리스트

- [ ] `git log --oneline -1`이 최신 커밋을 가리키는지
- [ ] `docker compose logs -f infinite_buying`에서 CRITICAL 에러 없이 프리장/본장 작업이 도는지
- [ ] 대시보드(`:8000`)에 접속되고 "아직 시작 전" 또는 정상 사이클 정보가 표시되는지
- [ ] `DRY_RUN=true` 로그에서 계산된 주문가/수량이 상식적인 범위인지 (설계도 별% 공식과 대략 맞는지 눈으로 확인)
- [ ] 개발 노트북의 자동매매 프로세스가 완전히 꺼져 있는지 (8장)
- [ ] 실주문 전환(`DRY_RUN=false`) 전에 텔레그램 등 실시간 알림 채널을 붙였는지 (현재는 `LogNotifier`만 있어 CRITICAL 알림을 놓칠 수 있음 — 구현설명서 2-c절 참고)
