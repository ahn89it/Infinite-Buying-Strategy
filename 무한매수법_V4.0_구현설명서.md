# 무한매수법 V4.0 자동매매 프로그램 구현설명서

> 이 문서는 `무한매수법_V4.0_자동매매_설계도.md`를 기반으로 실제 구현된 `infinite_buying_v4/`
> 코드의 동작 원리와, 실거래 전에 반드시 확인해야 할 사항을 정리한 문서입니다. 설계도가
> "무엇을 만들지"를 정의한 문서라면, 이 문서는 "실제로 무엇이 어떻게 만들어졌는지"와
> "사용하기 전에 무엇을 점검해야 하는지"를 다룹니다.
>
> **주의**: 키움증권 모의투자는 해외주식(미국주식)을 지원하지 않습니다. 이 프로그램은
> TQQQ(미국주식)만 다루므로 모의투자 계좌로 검증하는 것 자체가 불가능하고, 그래서 이
> 프로젝트에는 모의/실투자 전환 옵션이 없습니다(항상 실투자 API만 사용). 실주문 없이
> 로직을 검증하려면 `DRY_RUN=true` 설정을 사용하세요(2-d, 운영 런북 7장 참고).
>
> **장애 발생 시 복구 절차, 설정값 전수표, DB/이벤트로그 스키마 부록, 멱등성·드라이런·
> 부분체결 판정 등 운영 관점의 상세 내용은 `무한매수법_V4.0_운영_런북.md`를 참고하세요.
> 다른 PC(예: 실제 자동매매를 상시 실행할 노트북)에 새로 설치하는 절차는
> `무한매수법_V4.0_신규_노트북_설정_가이드.md`를 참고하세요.**

---

## 0. 요약

| 항목 | 내용 |
|---|---|
| 브로커 연동 | 키움증권 REST API (`kwcli` 공식 패키지, PyPI 배포) |
| DB | SQLite (`data/infinite_buying.db`) |
| 이벤트 로그 | JSONL append-only (`data/event_log.jsonl`) |
| 배포 | Docker + docker-compose |
| 분할수 | 20 / 40 (환경설정으로 전환, 기본 40) |
| 모의/실투자 | **없음 — 항상 실투자 API만 사용** (키움 모의투자는 해외주식 미지원). 사전 검증은 `DRY_RUN=true`로 |
| 사이클 재시작 | 복리(기본) / 단리 (환경설정으로 전환) |
| 테스트 | `pytest`, 119개 단위테스트 통과 (formulas/state/trade_history/event_log/normal_mode/reverse_mode/market_hours/scheduler/verify_replay/dashboard) |
| 대시보드 | Flask + SQLite 읽기 전용 조회, `docker-compose`에 별도 서비스로 등록 |
| 미구현 | 알림 채널(텔레그램/이메일 실제 구현체) |

---

## 1. 모듈별 로직 설명

### 1-1. `formulas.py` — 순수 계산 함수

설계도 2~4번의 수식을 부수효과 없는 순수함수로 구현했습니다. 모든 금액/T값 계산은
`Decimal`을 사용해 float 누적오차를 방지합니다.

- `star_percent(T, split_count)`: 별% = `15 - (30/split_count) * T`. 40분할(계수 0.75)과
  20분할(계수 1.5) 두 공식이 이 일반식 하나로 정확히 일치함을 단위테스트로 T=0~40
  전 구간 검증했습니다.
- `star_price()` / `buy_trigger_price()` / `sell_trigger_price()`: 별지점, 매수점(별지점-0.01),
  매도점(=별지점)을 센트 단위로 반올림해 계산합니다.
- `is_first_half(T, split_count)`: T ≤ split_count/2 → 전반전.
- `is_reverse_trigger(T, split_count)`: T > split_count-1 → 리버스모드 진입 트리거.
- `single_buy_amount(remaining_cash, T, split_count)`: 1회매수금 = `remaining_cash / (split_count - T)`.
  분모가 0 이하면 **`FormulaError`를 던져** 호출부가 리버스모드 전환을 먼저 확인하도록 강제합니다
  (조용히 잘못된 금액을 계산하지 않음).
- `new_average_price()`: 매수 체결 후 가중평균 평단가 계산.
- `return_pct()` / `profit_amount()`: 매도 건별 수익률/수익금.

### 1-2. `state.py` — 상태 영속화

설계도 1번의 State(모드, phase, T, 평단, 보유수량, 잔금 등)를 불변(`frozen=True`) 데이터클래스로
정의하고 SQLite에 저장/로드합니다.

**핵심 안전장치**: `load_state()`는 상태 행이 없으면 **절대 기본값을 만들지 않고 예외
(`StateError`)를 던집니다.** 신규 시작은 오직 `bootstrap.py`를 사람이 명시적으로 1회
실행해야만 가능합니다(`bootstrap_new_state()`). 스케줄러의 일상적인 자동 실행 경로는
이 규칙을 절대 우회하지 않습니다.

`start_new_cycle()`은 사이클 종료(보유수량 0) 후 다음 사이클을 시작합니다:
- 복리(`compound_on_restart=True`): 새 원금 = 직전 사이클 종료 시점 `remaining_cash`
- 단리(`False`): 새 원금 = `.env`의 `PRINCIPAL` 고정값

`validate()`가 저장/로드 시마다 호출되어 "REVERSE 모드인데 phase가 있음", "보유 0인데
평단가가 남아있음" 같은 모순된 상태를 즉시 걸러냅니다.

### 1-3. `event_log.py` — T값 재생(replay) 이벤트 소싱

설계도 2번 표를 그대로 구현했습니다. 체결 이벤트를 **append-only JSONL**에 기록하고,
그 이벤트들을 순서대로 "재생"해서 T값을 갱신합니다(중간 값을 실수로 고쳐쓰는 사고를
구조적으로 방지하기 위해 UPDATE 가능한 SQLite가 아닌 JSONL 파일을 씀).

| 이벤트 | T값 변화 |
|---|---|
| `FULL_BUY` (1회 매수 전액 체결) | `T += 1` |
| `HALF_BUY` (절반 매수 체결) | `T += 0.5` |
| `QUARTER_SELL` (쿼터매도) | `T *= 0.75` |
| `LIMIT_SELL_THEN_LOC_BUY_FULL` | `T = T*0.25 + 1` |
| `LIMIT_SELL_THEN_LOC_BUY_HALF` | `T = T*0.25 + 0.5` |

`state.py`의 T값은 이 이벤트 로그를 재생해서 얻은 결과를 캐싱해둔 것이고, 진짜 원천
데이터(source of truth)는 이 이벤트 로그 파일입니다. 문제가 생기면 이 파일을 처음부터
재생해서 T값이 맞는지 언제든 재검증할 수 있습니다.

### 1-4. `normal_mode.py` — 일반모드 매수/매도 주문 생성

설계도 5, 6번을 구현했습니다. **실제 주문을 내지 않고, "이런 주문을 내야 한다"는
`OrderIntent` 목록만 계산**합니다(네트워크 없이 100% 단위테스트 가능하도록 분리).

- **첫매수** (`generate_first_buy_orders`): "큰수" 미끼 LOC 주문(전일종가+12%, 증권사 ±20%
  주문제한 우회용, 체결 목적 아님) + 그 아래로 전일종가에서 3%씩 내려가는 4단계 실체결
  노림 LOC 주문(1회매수금을 4등분).
- **전반전** (`generate_first_half_buy_orders`): 1회매수금의 절반은 별지점, 절반은 평단가에 LOC.
- **후반전** (`generate_second_half_buy_orders`): 1회매수금 전액을 별지점(평단보다 낮음)에 LOC.
  T가 이미 리버스 트리거 조건이면 `NormalModeError`를 던져 `reverse_mode.py`로의 전환을 강제합니다.
- **매도** (`generate_sell_orders`, 전반전/후반전 공통): 보유수량 1/4 → 별지점 LOC(쿼터매도),
  나머지 3/4 → 평단+15% 지정가.
- **보조(추가 하락 대응) 사다리 주문** (`generate_aux_ladder_buy_orders`): **기본적으로
  스케줄러가 호출하지 않는 옵션 함수**입니다. 설계도가 "단계별 보조 주문"의 존재만
  언급하고 정확한 가격 간격/예산 배정 규칙을 명시하지 않았기 때문에, 임의로 실제
  현금을 배정하는 로직을 기본 동작에 넣지 않았습니다. 필요하면 이 함수를 직접
  호출하도록 스케줄러를 확장해야 합니다.

### 1-5. `reverse_mode.py` — 리버스(소진)모드

설계도 7번 구현.

- `reverse_divisor(split_count)`: 20분할→10등분, 40분할→20등분.
- `reverse_sell_qty()`: 보유수량 // 등분수. **0으로 계산되면 최소 1주로 보정**(설계도 11번
  방어 코드).
- `reverse_star_price()`: 직전 5거래일 종가 평균.
- D1(`generate_day1_moc_sell_order`): MOC 무조건 매도.
- D2 이후(`generate_daily_loc_sell_order`): 5거래일 평균 위쪽 LOC 매도. 매일 남은 보유수량
  기준 등분을 재계산하므로 자연히 매도수량이 줄어듭니다.
- `is_reverse_exit_condition()`: 종가 ≥ 평단가×0.85 → 다음 거래일부터 일반모드 복귀.

### 1-6. `market_hours.py` — 장 시간대/서머타임 판별

수동으로 한국시간 17시/18시를 분기하는 대신, 표준 라이브러리 `zoneinfo`로
`America/New_York` 타임존을 직접 다룹니다. 서머타임 계산은 OS의 tzdata가 대신 처리하므로,
서머타임 시작/종료 날짜를 코드 어디에도 하드코딩하지 않았습니다.

- 프리장 04:00~09:30 / 본장 09:30~16:00 / 애프터 16:00~20:00 / 주간거래 20:00~04:00 (ET 기준,
  코드 내부 값). **한국시간(KST)으로 환산한 실행 시간표는 운영 런북 1장 "실행 시간표"
  참고** — 서머타임 여부에 따라 KST 값이 달라지므로 이 문서에서는 코드가 실제로 비교하는
  ET 값만 적었습니다.
- `can_place_loc_or_moc_order()`: 본장에만 True.
- `can_place_limit_sell_order()`: 프리장/본장/애프터 True, 주간거래는 항상 False.
- 주말(`is_weekday`)도 함께 체크합니다. 단, **미국 증시 휴장일(추수감사절 등) 캘린더는
  다루지 않습니다** — 별도 데이터 소스 연동이 필요합니다(1-b "확인 필요" 항목 참고).

### 1-7. `trade_history.py` — 사람이 보는 거래 이력 (SQLite)

설계도 9번 구현. `event_log.py`(내부 T값 재생용)와 별도로, 사람이 조회하는
매수/매도 이력, 사이클 요약, 전체 누적 성과를 SQLite에 기록합니다.

- `record_buy()` / `record_sell()`: 체결 확인된 건만 기록(미체결은 저장 안 함). 매도는
  `formulas.return_pct()`/`profit_amount()`로 건별 수익률/수익금을 함께 계산.
- `open_cycle_summary()` / `close_cycle_summary()`: 사이클 시작 시 "진행 중" 행 생성,
  종료 시 buy_records/sell_records를 Decimal로 직접 합산(SQL `SUM()`은 float 취급이라
  쓰지 않음)해 사이클 손익/수익률/소요일수를 확정.
- `ensure_portfolio_summary()` / `update_portfolio_summary()`: 계좌 전체 누적 실현손익
  (완료된 사이클들의 합), 미실현 손익, 총평가금액, 총수익률을 계산.
- `export_to_csv()`: 4개 테이블을 각각 CSV로 내보내는 함수(엑셀 조회용).

### 1-8. `kiwoom_adapter.py` — 키움 REST API 연동

공식 `kwcli` 패키지(`import kiwoom`)가 제공하는 OAuth 토큰 발급/갱신, REST 호출,
WebSocket 실시간 구독 하부구조를 그대로 사용하고, 그 위에 무한매수법 전용 함수를 얹었습니다.

| 함수 | 키움 API | 용도 |
|---|---|---|
| `submit_order()` / `submit_order_with_retry()` | `ust20000`(매수)/`ust20001`(매도) | OrderIntent를 실제 주문으로 제출, 실패 시 선형 백오프 재시도 |
| `cancel_order()` / `cancel_all_open_orders()` | `ust20003` | 미체결 주문 취소 |
| `get_today_fills()` | `ust21510` | 당일 체결 조회 |
| `get_open_orders()` | `ust21050` | 미체결 주문 목록 (일일 주문 갱신 전 정리용) |
| `get_usd_deposit()` | `ust21110` | USD 예수금 조회 |
| `get_quote()` | `usa20100` | 현재가/전일종가 |
| `get_recent_daily_closes()` | `usa06012` | 일봉 차트 → 직전 N거래일 종가 |
| `subscribe_realtime_fills()` | WebSocket F5 | 실시간 체결통보 구독(장기 실행 코루틴) |

**주문유형 매핑**: `LOC→trde_tp "30"`, `LIMIT→"00"`, `MOC→"33"` (공식 예제 문서 주석 근거).

### 1-9. `notifier.py` — 알림 인터페이스

설계도 11번 "자동매매 중단 + 알림" 요구사항의 **인터페이스만** 구현했습니다
(사용자 요청에 따라 채널 구현은 보류). `NotifierBase` 추상클래스 + `LogNotifier`(파이썬
logging으로만 기록)가 현재 유일한 구현체입니다. 나중에 `TelegramNotifier`,
`EmailNotifier` 등을 이 파일에 추가하면 다른 코드는 전혀 건드릴 필요가 없습니다.

### 1-10. `scheduler.py` — 일일 실행 오케스트레이션

모든 모듈을 실제로 엮어서 하루 두 번 실행합니다. 실행 시각은 코드상 ET(America/New_York)
04:00/09:30에 고정되어 있고, 한국시간으로는 서머타임 여부에 따라 17시/18시,
22:30/23:30이 됩니다 — 정확한 KST 값과 서머타임 판별 방법은 운영 런북 1장 "실행
시간표" 참고.

1. **`run_premarket_update()`** (ET 04:00 = KST 17:00/18:00, 프리장 시작)
   - 전일 미체결 주문 정리 → 전일 체결 조회 → 체결을 T값 갱신 이벤트로 분류(`_classify_daily_events`)
     → `event_log`에 기록 및 T 재계산 → 평단/보유수량/잔금 갱신 → `trade_history` 기록
   - 사이클 종료/리버스모드 진입·이탈 판정 및 전이
   - 상태 저장
   - 보유 중이면 그날의 지정가매도(+15%) 주문 제출
   - `portfolio_summary` 갱신
2. **`run_regular_session_orders()`** (ET 09:30 = KST 22:30/23:30, 본장 시작)
   - 그날의 LOC/MOC 매수·매도 주문 생성(모드/phase에 따라 `normal_mode.py` 또는
     `reverse_mode.py` 호출) 및 제출

`start_scheduler()`가 APScheduler로 두 작업을 America/New_York 타임존 고정 cron으로
등록합니다. 두 번으로 나눈 이유는 설계도 6번이 "지정가매도=프리장부터", "LOC/MOC=본장 중"으로
제출 가능 시간대를 다르게 명시했기 때문입니다.

**체결→이벤트 분류 로직**(`_classify_daily_events`)은 설계도 2번 규칙 중 가장 해석의
여지가 컸던 부분입니다. `submitted_orders`라는 내부 장부 테이블(설계도 스키마에는
없는, 이 프로젝트가 추가한 운영용 테이블)에 그날 제출한 주문의 `purpose`를 기록해뒀다가,
체결 시 대조해서 "이 체결이 어떤 종류의 매수/매도였는지"를 판정합니다. "지정가매도 후
같은 날 LOC매수 동시체결" 조합이 감지되면 **WARNING 알림을 보내 자동판정 결과를 사람이
한 번 더 확인**하도록 했습니다.

### 1-11. `bootstrap.py` — 신규 시작 스크립트

`python -m infinite_buying_v4.bootstrap`으로 1회 실행. T=0/보유0 상태, 사이클 1번,
포트폴리오 요약 초기 행을 생성합니다. 이미 상태가 있으면 실수로 덮어쓰지 않도록 예외를 던집니다.

### 1-12. `dashboard/` — 로컬 웹 대시보드

설계도 12번 구현. `scheduler.py`(자동매매)와 완전히 별도 프로세스로 떠서, SQLite를
**읽기 전용으로만** 조회합니다(설계도 12-1번 "별도 계산 로직 중복 금지").

- **`dashboard/data.py`**: Flask에 대한 의존이 전혀 없는 순수 조회 계층. `get_portfolio_summary()`,
  `get_current_cycle_status()`, `get_recent_trades()`, `get_cycle_history()`를 하나로 묶는
  `build_dashboard_payload()`가 `/api/dashboard`가 반환하는 JSON 전체를 조립합니다.
  - **"현재가" 표시 방식**: 대시보드는 키움 API를 직접 호출하지 않습니다. 대신
    `portfolio_summary.current_unrealized_pnl = (현재가 − 평단가) × 보유수량` 관계를 거꾸로 풀어
    `현재가 = 평단가 + 미실현손익 / 보유수량`으로 역산합니다. 이 값은 `scheduler.py`가 마지막으로
    `portfolio_summary`를 갱신한 시점의 스냅샷이며, 진짜 실시간 시세가 아닙니다(UI에도 이 사실을
    작게 표기).
  - **다음 매수/매도 목표가**: `formulas.py`의 `buy_trigger_price()`/`sell_trigger_price()`를
    그대로 재사용해 현재 상태(T, 평단가, 분할수)로부터 계산합니다. 리버스모드는 매도가 산출에
    직전 5거래일 종가(실시간 조회 필요)가 있어야 해서, 대시보드에서는 목표가를 보여주지 않고
    안내 문구만 표시합니다.
- **`dashboard/server.py`**: Flask 앱. 라우트는 `GET /`(정적 `index.html`)와
  `GET /api/dashboard`(JSON) 두 개뿐이며, 라우트 코드에는 쿼리 로직이 없습니다(전부 `data.py`).
  기본적으로 `0.0.0.0`에 바인딩해 같은 공유기 내부망의 다른 기기(스마트폰 등)에서도 접속할 수
  있게 했습니다(외부 인터넷 노출/인증은 설계도 범위 밖).
- **`dashboard/static/index.html`**: 바닐라 JS + Chart.js(CDN) 단일 페이지. 60초마다
  `/api/dashboard`를 폴링해 갱신합니다(설계도 12-1번 "최소 몇 분 간격으로 자동 갱신"). 표시 항목은
  설계도 12-2번 그대로: 상단 요약 카드, 현재 사이클 현황, 최근 거래 20건 테이블, 완료된
  사이클별 수익률 막대그래프(설계도 12-2번이 제시한 두 차트 옵션 중 "사이클별 막대그래프" 채택 —
  누적 자산 곡선은 시계열 스냅샷 테이블이 없어 이번 구현에는 포함하지 않음).
- **`docker-compose.yml`**: `infinite_buying`과 같은 `./data` 볼륨을 공유하는 `dashboard`
  서비스를 추가했습니다. `DASHBOARD_PORT`(.env, 기본 8000)를 호스트에 그대로 노출합니다.

실행: `python -m infinite_buying_v4.dashboard.server` (로컬) 또는 `docker compose up -d dashboard`
(Docker) 후 `http://localhost:8000`(또는 설정한 포트) 접속.

---

## 2. 사용 전 반드시 확인해야 할 사항

### 2-a. 필수 준비 (사용자가 직접 해야 함, 자동화 불가)

1. 키움증권 OpenAPI 포털에서 **실투자용** App Key/Secret 발급 (모의투자는 해외주식을
   지원하지 않아 발급 자체가 불필요/불가능합니다).
2. 프로젝트 루트에 `.env.example`을 복사해 `.env` 생성 후 값 채우기:
   - `APP_KEY`, `APP_SECRET`
   - `DRY_RUN=true` (아래 2-b/2-d, 운영 런북 7장 — 실주문 없이 먼저 검증)
   - `SPLIT_COUNT`(20 또는 40), `PRINCIPAL`(원금, USD), `COMPOUND_ON_RESTART`
3. `python -m infinite_buying_v4.bootstrap` 1회 실행 → 신규 상태 생성 확인.
4. `pip install -r requirements.txt` (로컬 실행 시) 또는 `docker compose build` (Docker).

### 2-b. 코드에 "실거래 전 검증 필요"로 명시해둔 가정들

이 항목들은 설계도가 정확한 값을 주지 않았거나, 제가 공식 문서를 100% 확신할 수
없어서 **합리적 추정치로 구현하고 주석에 명시**해둔 부분입니다. 키움 모의투자가 해외주식을
지원하지 않아 별도 샌드박스 계좌로 미리 돌려볼 수 없으므로, **`DRY_RUN=true`로 최소
1~2주 이상 로그를 확인**하고, 그다음 최소 수량으로 `DRY_RUN=false` 실주문을 1건씩
늘려가며 검증하는 것을 강력히 권장합니다.

| 위치 | 가정한 내용 | 확인 방법 |
|---|---|---|
| `market_hours.py` | 주간거래(데이마켓) 시간대를 20:00~04:00 ET(한국시간 기준 서머타임 09:00~17:00·비서머타임 10:00~18:00, 다음날)로 추정 | 키움증권 공식 공지의 "주간거래 운영시간" 확인 후 `DEFAULT_DAY_MARKET_START_ET`/`END_ET` 조정 |
| `kiwoom_adapter.py` | 실시간 체결(F5) 필드 `907`(매도수구분) 값이 REST의 `slby_tp`(1=매도,2=매수)와 동일한 규칙이라고 가정 | `DRY_RUN=true`여도 실시간 체결 구독 자체는 실계좌 데이터이므로, 실제 수신 이벤트를 로그로 찍어 확인 |
| `kiwoom_adapter.get_today_fills()` | "당일 체결"이 스케줄러 실행 시점 기준 정말 최신 세션까지 포함하는지 미확인 | 최소 수량 실주문 체결 후 다음 프리장 실행 로그에서 정상 반영되는지 확인 |
| `scheduler._classify_daily_events()` | "지정가매도 후 같은 날 LOC매수 체결" 조합 및 쿼터매도와의 동시발생 케이스 | 이 케이스 발생 시 WARNING 알림이 오므로, 발생하면 T값이 올바른지 수동 검산 권장. 부분체결 판정 로직 자체(체결금액 비율 계산)는 2026-08-03에 버그가 수정됐습니다 — 자세한 내용은 운영 런북 5장 |
| `normal_mode.py` 첫매수 사다리, 보조 하락대응 주문 | 정확한 가격 간격/단수를 설계도가 명시하지 않아 임의 기본값(3%씩 4단계) 사용, 보조 사다리는 기본 비활성 | 필요 시 `DEFAULT_FIRST_BUY_LADDER_STEPS`/`STEP_PCT` 조정 또는 `generate_aux_ladder_buy_orders()`를 직접 스케줄러에 연결 |
| `market_hours.py` | 미국 증시 휴장일(추수감사절 등) 캘린더 미반영 | 필요 시 별도 휴장일 데이터 연동 추가 필요 (이번 구현 범위 밖) |

### 2-c. 아직 구현되지 않은 것

- **알림 채널 실제 구현체**(텔레그램/이메일): 현재는 `LogNotifier`(로그 기록)만 있어,
  CRITICAL 알림을 사람이 실시간으로 놓칠 수 있습니다. **`DRY_RUN=false`로 실주문을
  시작하기 전에는 최소한 텔레그램 봇 알림 정도는 구현해서 붙이는 것을 권장**합니다.
- **해외 환전(FX) 자동화**: 원금이 이미 USD 예수금으로 있다는 전제로 구현했습니다.
  원화→USD 자동 환전 로직은 포함되지 않았습니다.
- **미국 증시 휴장일 캘린더**: 위 2-b 참고.

### 2-d. 2026-08-03 추가된 운영 안전장치

이 절 이후에 추가된 기능입니다. 각 항목의 판정 키/사용법/장애 대응 절차는 운영 런북에
상세히 정리되어 있습니다.

- **멱등성**: `daily_run_log` 테이블로 컨테이너 재시작 시 같은 날 주문이 중복 제출되지
  않도록 방어 (운영 런북 6장).
- **드라이런 모드**: `.env`의 `DRY_RUN=true`로 실제 주문 제출 없이 로그만 남기는 모드
  (운영 런북 7장).
- **이벤트 재생 검증 CLI**: `python -m infinite_buying_v4.verify_replay`로 T값이 이벤트
  로그 재생 결과와 일치하는지 검증/복구 (운영 런북 8-3장).
- **스케줄러 예외 정책**: 처리되지 않은 예외가 나면 실제로 스케줄러가 종료되도록
  `EVENT_JOB_ERROR` 리스너 추가 (운영 런북 4장).

### 2-e. 실행 방법 요약

> 처음 실행할 때는 `.env`에 `DRY_RUN=true`를 켜둔 채로 `scheduler`를 돌려 로그만
> 확인하세요. 문제 없음을 확인한 뒤에만 `DRY_RUN=false`로 바꿔 실주문을 시작하세요.

```bash
# 로컬 실행
pip install -r requirements-dev.txt   # 테스트까지 포함
python -m pytest infinite_buying_v4/tests/ -q
python -m infinite_buying_v4.bootstrap        # 최초 1회만
python -m infinite_buying_v4.scheduler        # 상시 실행 (프리장/본장 자동 트리거)

# Docker 실행
docker compose build
docker compose run --rm infinite_buying python -m infinite_buying_v4.bootstrap   # 최초 1회만
docker compose up -d                 # infinite_buying(자동매매) + dashboard 둘 다 기동
docker compose logs -f infinite_buying    # 자동매매 로그
docker compose logs -f dashboard          # 대시보드 로그
# 대시보드 접속: http://localhost:8000 (DASHBOARD_PORT 기본값)
```

---

## 3. 테스트 현황

`pytest infinite_buying_v4/tests/` 기준 **119개 테스트 전부 통과**(2026-08-03 기준):

| 테스트 파일 | 개수 | 검증 범위 |
|---|---|---|
| `test_formulas.py` | 16 | 별%/1회매수금 20·40분할 T=0~40 전 구간, 경계값, 방어코드 |
| `test_state_transition.py` | 11 | 상태 없음 예외, bootstrap, 저장/로드 정밀도, 복리/단리 재시작 |
| `test_trade_history.py` | 9 | 매수/매도 기록, 사이클 집계, 포트폴리오 누적, CSV 내보내기 |
| `test_event_log.py` | 13 | T값 갱신 5개 규칙, append/read, 손상 데이터 감지 |
| `test_mode_switch.py` | 13 | 일반모드 매수/매도 생성, 리버스 전환 경계값 |
| `test_reverse_mode.py` | 12 | 등분수, 최소 1주 보정, D1/D2+ 주문, 종료조건 경계값 |
| `test_market_hours.py` | 12 | 서머타임 자동판별, 장 시간대, 주문 가능 여부 |
| `test_scheduler.py` | 17 | 멱등성 가드, 드라이런 배선, 부분체결 분류 로직(키움 REST 호출은 전부 mock) |
| `test_verify_replay.py` | 5 | 이벤트 재생 T값 검증/복구 CLI |
| `test_dashboard_data.py` | 11 | 요약/현재 사이클/최근거래/사이클 히스토리 조회, 현재가 역산, bootstrap 전 None 처리 |

`kiwoom_adapter.py`의 실제 REST/WebSocket 호출부만 진짜 키움 API 인증이 필요해
단위테스트 대상에서 제외했습니다. 키움 모의투자가 해외주식을 지원하지 않아 이 부분은
사전 샌드박스 검증이 불가능하므로, `DRY_RUN=true`로 실계좌 시세·체결 조회 로직을
먼저 확인한 뒤(운영 런북 7장), 최소 수량 실주문으로 직접 검증해야 합니다.
