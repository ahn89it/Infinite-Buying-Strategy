# 무한매수법 V4.0 운영 런북 (장애 대응 & 설정 가이드)

## 문서 메타

| 항목 | 내용 |
|---|---|
| 버전 | 1.2 |
| 작성일 | 2026-08-03 |
| 대상 코드 버전 | `daily_run_log`/`DRY_RUN`/`verify_replay.py` 도입 + 모의투자 모드 제거 + `dashboard/` 구현 이후 (119개 단위테스트 통과 시점) |
| 관련 문서 | `무한매수법_V4.0_자동매매_설계도.md`(방법론 스펙), `무한매수법_V4.0_구현설명서.md`(모듈별 구현 설명) |

### 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-08-01 | 최초 구현 (formulas~scheduler~Docker), 86개 테스트 |
| 2026-08-03 | 멱등성(`daily_run_log`), 드라이런(`DRY_RUN`), `verify_replay.py` CLI 추가. **`_classify_daily_events()`의 부분체결 판정 버그 수정**(형제 주문 미체결분이 `buy_intended` 계산에서 누락되던 문제). 108개 테스트 |
| 2026-08-03 (2차) | **모의투자(KIWOOM_MODE=demo) 관련 코드/설정/문서 전면 제거.** 키움증권 모의투자는 해외주식(미국주식)을 지원하지 않아, 이 프로젝트(TQQQ 대상)에는 애초에 모의투자로 검증할 방법이 없다는 것이 확인됨. `APP_KEY_MOCK`/`APP_SECRET_MOCK`/`KIWOOM_MODE` 설정 제거, 항상 실투자 API만 사용하도록 고정. `DRY_RUN`이 유일한 사전 검증 수단이 됨 |
| 2026-08-03 (3차) | **`dashboard/` 구현** (설계도 12번). Flask + SQLite 읽기 전용 조회, `docker-compose.yml`에 `dashboard` 서비스 추가. 신규 테스트 11건 (119개) |

---

## 1. 용어집

| 용어 | 의미 |
|---|---|
| **T값** | 무한매수법의 진행 회차를 나타내는 소수 가능한 값. 매수/매도 이벤트마다 정해진 규칙(아래 5장)으로 갱신됨. `state.t`에 저장, `event_log.jsonl` 재생으로 언제든 재검증 가능 |
| **별지점** | 그날의 매수/매도 목표가. `평단가 × (1 + 별% / 100)`. 별%는 T값과 분할수로 계산 |
| **매수점 / 매도점** | 매수점 = 별지점 − 0.01달러(매도점과 가격 겹침 방지), 매도점 = 별지점 그대로 |
| **전반전 / 후반전** | T ≤ 분할수/2 면 전반전(별지점이 평단보다 위), T > 분할수/2 면 후반전(별지점이 평단보다 아래) |
| **큰수(미끼) 주문** | 첫매수 시 전일종가+12% 가격에 내는 실체결 목적이 아닌 LOC 주문. 증권사 ±20% 주문가 제한 우회용. `OrderIntent.is_decoy=True` |
| **쿼터매도** | 보유수량의 1/4을 별지점에서 LOC로 매도. T값을 0.75배로 감소시킴 |
| **지정가매도** | 나머지 3/4을 평단가+15%에 지정가로 매도. 프리장 시작 시각부터 애프터마켓까지 유지 |
| **리버스모드(소진모드)** | T > 분할수−1이 되면 진입. 매수를 멈추고 보유 물량을 등분해서 순차 매도 |
| **사이클** | T=0·보유0에서 시작해 다시 보유0이 될 때까지의 한 주기. 종료 시 `cycle_summary`에 집계되고 새 사이클(`cycle_id+1`)이 시작됨 |
| **LOC / MOC / 지정가(LIMIT)** | LOC=종가지정가(장마감 때 지정가 이하/이상이면 체결), MOC=종가시장가(무조건 체결), LIMIT=일반 지정가 |
| **드라이런(dry-run)** | 실제 주문 제출 없이 `OrderIntent` 계산 결과만 로그로 남기는 모드(`.env`의 `DRY_RUN=true`). 이 프로젝트에는 모의투자가 없으므로, 실거래 전 로직을 점검하는 **유일한** 수단 |
| **모의투자(demo)** | 키움증권이 제공하는 가상계좌 서비스. **해외주식(미국주식)을 지원하지 않아** 이 프로젝트(TQQQ 대상)에는 사용할 수 없음. 코드/설정에 모의투자 관련 옵션이 존재하지 않음(항상 실투자 API만 사용) |

### 시각 표기: ET ↔ KST 병기

이 프로젝트는 내부적으로 전부 America/New_York(ET) 기준으로 동작합니다(`market_hours.py`, `zoneinfo` 사용, 서머타임 자동 반영). 사람이 참고할 때는 아래 표를 씁니다 — **서머타임 여부에 따라 KST 환산이 달라지는 것에 주의**하세요.

| 세션 | ET 시각 | KST (서머타임/EDT, 3월~11월경) | KST (비서머타임/EST, 11월~3월경) |
|---|---|---|---|
| 프리장 시작 | 04:00 | 17:00 | 18:00 |
| 본장 시작 | 09:30 | 22:30 | 23:30 |
| 본장 종료 | 16:00 | 05:00 (익일) | 06:00 (익일) |
| 애프터 종료 | 20:00 | 09:00 (익일) | 10:00 (익일) |
| 주간거래(데이마켓) | 20:00~04:00 | 09:00~17:00 (익일 기준 재정렬 필요) | 10:00~18:00 |

> 위 KST 값은 참고용 근사치입니다. 실제 서머타임 전환일 전후 며칠은 `python -c "from infinite_buying_v4.market_hours import now_et, is_dst_now; print(now_et(), is_dst_now())"` 로 직접 확인하세요.

---

## 2. 설정(.env) 전체 표

| 변수명 | 기본값 | 허용 범위/형식 | 필수 | 영향 |
|---|---|---|---|---|
| `APP_KEY` / `APP_SECRET` | 없음 | 문자열 | **예** | 키움 REST API 실투자 계좌 인증. 이 프로젝트에는 모의투자 옵션이 없으므로(아래 참고) 항상 실투자용 키만 사용 |
| **`DRY_RUN`** | `false` | `true` \| `false` | 아니오 | **드라이런 모드**. true면 실제 주문 제출 없이 로그만 남김 (7장 참고). 모의투자가 없는 이 프로젝트의 유일한 사전 검증 수단이므로, 처음 운영을 시작할 때는 반드시 `true`로 시작할 것 |
| **`TICKER`** | `TQQQ` | 문자열(종목코드) | 아니오 | **매매 대상 종목**. 다른 종목(예: SOXL)으로 바꾸려면 이 값만 변경하면 됨. 단, 별%/원금 공식 자체는 TQQQ 기준으로 설계되어 있어 다른 종목은 설계도 재검토 필요 |
| `EXCHANGE_CODE` | `ND` | `NA`(AMEX) \| `ND`(NASDAQ) \| `NY`(NYSE) | 아니오 | 키움 REST의 거래소구분 코드. TICKER를 바꾸면 이 값도 그 종목의 실제 상장 거래소에 맞게 바꿔야 함 |
| `SPLIT_COUNT` | `40` | `20` \| `40` | 아니오 | 분할수. 별%/1회매수금 공식의 계수를 결정 |
| `PRINCIPAL` | 없음 | 양수 Decimal (USD) | **예** | 총 원금. 신규 시작(`bootstrap.py`) 시점의 초기 원금, 단리 재시작 시 고정 원금으로도 사용됨 |
| `COMPOUND_ON_RESTART` | `true` | `true` \| `false` | 아니오 | 사이클 재시작 시 복리(이전 잔금 이어받음)/단리(PRINCIPAL 고정) |
| `DB_PATH` | `<프로젝트루트>/data/infinite_buying.db` | 파일 경로 | 아니오 | SQLite DB 위치 |
| `EVENT_LOG_PATH` | `<프로젝트루트>/data/event_log.jsonl` | 파일 경로 | 아니오 | 이벤트 로그(JSONL) 위치 |
| `DASHBOARD_PORT` | `8000` | 정수 | 아니오 | `dashboard/server.py`(Flask)가 바인딩하는 포트. `docker-compose.yml`의 `dashboard` 서비스가 호스트에 동일 포트로 노출 |

**모의투자를 지원하지 않는 이유**: 키움증권 모의투자 서비스는 해외주식(미국주식) 매매를
지원하지 않습니다. 이 프로젝트는 TQQQ(미국주식)만 다루므로, 모의투자 계좌 자체를 발급받을
수 없고 애초에 이 프로그램으로 접속할 대상 서버가 존재하지 않습니다. 그래서 `config.py`에는
`KIWOOM_MODE` 같은 real/demo 전환 로직이 없고, `kiwoom_adapter.py`는 항상
`https://api.kiwoom.com`(운영 REST)·`wss://api.kiwoom.com:10000`(운영 WebSocket) 고정
엔드포인트로만 연결합니다. 실거래 전 검증은 `DRY_RUN`(7장)으로 대체합니다.

**종목(티커) 변경 방법**: `.env`의 `TICKER`와 `EXCHANGE_CODE` 두 값만 바꾸면 됩니다. 예: `TICKER=SOXL`, `EXCHANGE_CODE=ND`. 코드 어디에도 `TQQQ`가 하드코딩되어 있지 않습니다(전부 `config.ticker`를 통해 참조).

---

## 3. 데이터 스키마 부록

### 3-1. SQLite DDL (`db.py`가 실제로 실행하는 스키마 전문)

```sql
CREATE TABLE IF NOT EXISTS state (
    id INTEGER PRIMARY KEY CHECK (id = 1),
    mode TEXT NOT NULL,                            -- "NORMAL" | "REVERSE"
    phase TEXT,                                     -- "FIRST" | "FIRST_HALF" | "SECOND_HALF" (NORMAL일 때만)
    split_count INTEGER NOT NULL,                   -- 20 | 40
    principal TEXT NOT NULL,                        -- Decimal 문자열
    remaining_cash TEXT NOT NULL,                    -- Decimal 문자열
    t_value TEXT NOT NULL,                           -- Decimal 문자열 (T값)
    avg_price TEXT NOT NULL,                         -- Decimal 문자열
    holding_qty INTEGER NOT NULL,
    cycle_id INTEGER NOT NULL,
    cycle_start_date TEXT NOT NULL,                  -- YYYY-MM-DD
    reverse_day_count INTEGER NOT NULL DEFAULT 0,
    reverse_prev_qty INTEGER NOT NULL DEFAULT 0,
    updated_at TEXT NOT NULL                         -- ISO8601
);

CREATE TABLE IF NOT EXISTS buy_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    cycle_id INTEGER NOT NULL,
    buy_date TEXT NOT NULL,                          -- YYYY-MM-DD
    buy_price TEXT NOT NULL,
    buy_qty INTEGER NOT NULL,
    buy_amount TEXT NOT NULL,
    order_type TEXT NOT NULL,                        -- FIRST | HALF_STAR | HALF_AVG | FULL_STAR | REVERSE
    t_after TEXT NOT NULL,
    avg_price_after TEXT NOT NULL,
    created_at TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS sell_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    cycle_id INTEGER NOT NULL,
    sell_date TEXT NOT NULL,
    sell_price TEXT NOT NULL,
    sell_qty INTEGER NOT NULL,
    sell_amount TEXT NOT NULL,
    order_type TEXT NOT NULL,                        -- QUARTER | LIMIT_15PCT | REVERSE_MOC | REVERSE_LOC
    avg_price_at_sell TEXT NOT NULL,
    return_pct TEXT NOT NULL,
    profit_amount TEXT NOT NULL,
    t_after TEXT NOT NULL,
    created_at TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS cycle_summary (
    cycle_id INTEGER PRIMARY KEY,
    start_date TEXT NOT NULL,
    end_date TEXT,                                   -- 진행 중이면 NULL
    total_buy_amount TEXT NOT NULL DEFAULT '0',
    total_sell_amount TEXT NOT NULL DEFAULT '0',
    cycle_profit_amount TEXT,
    cycle_return_pct TEXT,
    hit_reverse_mode INTEGER NOT NULL DEFAULT 0,      -- 0/1 (SQLite에 BOOLEAN 없음)
    duration_days INTEGER
);

CREATE TABLE IF NOT EXISTS portfolio_summary (
    id INTEGER PRIMARY KEY CHECK (id = 1),
    strategy_start_date TEXT NOT NULL,
    initial_principal TEXT NOT NULL,
    total_realized_profit TEXT NOT NULL DEFAULT '0',
    total_realized_return_pct TEXT NOT NULL DEFAULT '0',
    current_unrealized_pnl TEXT NOT NULL DEFAULT '0',
    current_unrealized_return_pct TEXT NOT NULL DEFAULT '0',
    total_equity TEXT NOT NULL,
    total_return_pct TEXT NOT NULL DEFAULT '0',
    completed_cycles INTEGER NOT NULL DEFAULT 0,
    last_updated TEXT NOT NULL
);

-- 운영용 부가 테이블 (설계도 9번 스키마에는 없음, scheduler.py 내부 장부)
CREATE TABLE IF NOT EXISTS submitted_orders (
    order_no TEXT PRIMARY KEY,
    submitted_date TEXT NOT NULL,
    side TEXT NOT NULL,                              -- BUY | SELL
    order_kind TEXT NOT NULL,                         -- LOC | LIMIT | MOC
    price TEXT,                                       -- MOC는 NULL
    qty INTEGER NOT NULL,
    purpose TEXT NOT NULL,                            -- buy_records/sell_records의 order_type과 동일 값
    is_decoy INTEGER NOT NULL DEFAULT 0
);

CREATE TABLE IF NOT EXISTS daily_run_log (
    run_type TEXT NOT NULL,                           -- PREMARKET | REGULAR
    run_date TEXT NOT NULL,                            -- YYYY-MM-DD (ET 기준)
    claimed_at TEXT NOT NULL,                           -- ISO8601
    PRIMARY KEY (run_type, run_date)
);

CREATE INDEX IF NOT EXISTS idx_buy_records_cycle_id ON buy_records (cycle_id);
CREATE INDEX IF NOT EXISTS idx_sell_records_cycle_id ON sell_records (cycle_id);
```

**테이블 역할 구분** (혼동하기 쉬운 두 테이블):

| 테이블 | 역할 | 언제 지워지나 |
|---|---|---|
| `submitted_orders` | "이 주문이 무슨 종류(purpose)였는지" 기억해뒀다가, 나중에 들어온 체결과 **대조**하는 용도 | 체결 매칭 성공 시 즉시 삭제. 취소되어 영영 체결 안 될 주문은 다음 프리장에서 `_purge_stale_submitted_orders()`가 정리 |
| `daily_run_log` | "오늘 이 작업(PREMARKET/REGULAR)을 이미 시작했는지" 그 자체를 기억하는 **멱등성 잠금** | 지우지 않음(영구 보관, 감사 로그 겸용). 강제 재실행하려면 운영자가 수동으로 해당 행만 삭제(8-1 참고) |

### 3-2. `event_log.jsonl` 레코드 필드

한 줄이 이벤트 하나(JSON), append-only. `Event` 데이터클래스(`event_log.py`) 정의 그대로:

| 필드 | 타입 | 의미 |
|---|---|---|
| `timestamp` | string (ISO8601, UTC) | 이 이벤트가 로그에 기록된 시각 |
| `event_date` | string (`YYYY-MM-DD`) | 체결일. 어느 사이클에 속하는지 판단하는 기준(`verify_replay.py`가 `cycle_start_date` 이상인 것만 재생) |
| `event_type` | string | 아래 5종 중 하나 |
| `price` | string \| null | 체결 단가 (감사용, T값 계산에는 안 씀) |
| `qty` | int \| null | 체결 수량 (감사용) |
| `note` | string \| null | 자유 메모 |

**`event_type` 5종과 T값 변화** (설계도 2번, `event_log.apply_event()`):

| event_type | T값 변화 | 발생 조건 |
|---|---|---|
| `FULL_BUY` | `T += 1` | 그날의 매수 라운드가 (거의) 전액 체결 |
| `HALF_BUY` | `T += 0.5` | 그날의 매수 라운드가 부분(절반 수준)만 체결 |
| `QUARTER_SELL` | `T *= 0.75` | 쿼터매도(1/4) 체결 |
| `LIMIT_SELL_THEN_LOC_BUY_FULL` | `T = T*0.25 + 1` | 지정가매도 체결 + 같은 날 LOC매수 전액 체결 |
| `LIMIT_SELL_THEN_LOC_BUY_HALF` | `T = T*0.25 + 0.5` | 지정가매도 체결 + 같은 날 LOC매수 부분 체결 |

예시 한 줄:
```json
{"timestamp": "2026-08-03T09:31:02.123456+00:00", "event_date": "2026-08-03", "event_type": "FULL_BUY", "price": "50.12", "qty": 20, "note": null}
```

---

## 4. 예외 처리 정책

**원칙 한 줄 요약**: `scheduler.py`가 처리하지 않고 밖으로 던지는 예외는 전부 "자동매매 중단" 대상입니다. 조용히 다음 거래일에 재시도하지 않습니다.

### 4-1. 예외별 처리표

| 예외 | 발생 위치 | 처리 방식 | 알림 |
|---|---|---|---|
| `ConfigError` | `load_config()` (스케줄러 시작 시점) | `start_scheduler()`가 잡아서 재발생(raise) → 프로세스 종료 | CRITICAL (notifier가 준비된 후 발생하므로 항상 전송됨) |
| `StateError` | `load_state()`, `save_state()` (매 실행 시작 시) | `_run_guarded`가 잡아서 재발생 → `EVENT_JOB_ERROR` → 스케줄러 종료 | CRITICAL |
| `FormulaError` | `formulas.py` 계산 함수 (예: 분모 0 이하) | 위와 동일 (호출 경로 어디서든 잡히지 않은 채 위로 전파됨) | CRITICAL |
| `NormalModeError` | `normal_mode.py` (예: 후반전 함수를 리버스 트리거 이후에 호출) | 위와 동일 | CRITICAL |
| `KiwoomAdapterError` (조회성 API: `get_today_fills`, `cancel_all_open_orders`, `get_quote` 등 필수 단계) | `run_premarket_update()` | 그 자리에서 CRITICAL 알림 후 즉시 재발생 → 스케줄러 종료 | CRITICAL |
| `KiwoomAdapterError` (`get_quote` 부가 단계: portfolio_summary 갱신용, 리버스 종료조건 판정용) | `run_premarket_update()`, `_handle_cycle_and_mode_transitions()` | **잡아서 WARNING만 남기고 계속 진행** (해당 갱신만 건너뜀) | WARNING |
| `KiwoomAdapterError` (개별 주문 제출, `submit_order_with_retry` 내부에서 재시도 소진) | `_submit_or_dry_run()` | 재시도(최대 3회, 선형 백오프) 후에도 실패하면 그 함수 내부에서 CRITICAL 알림 → 그 주문만 포기(`None` 반환) → **나머지 주문은 계속 제출 시도** | CRITICAL (주문 1건당) |
| 미끼(decoy) 주문 실제 체결 | `_classify_daily_events()` | 알림만, 예외 아님(중단하지 않음) — 사람이 확인해야 하는 이상 징후 | CRITICAL |
| 지정가매도+같은날LOC매수 복합 케이스 | `_classify_daily_events()` | 알림만, 예외 아님 — 자동 판정 결과를 사람이 재확인 | WARNING |
| `EventLogError` (JSONL 손상) | `verify_replay.py` 실행 시 | CLI가 종료코드 2로 종료, 별도 알림 없음(수동 실행 도구이므로) | 없음 (운영자가 직접 실행) |

### 4-2. "개별 주문 실패 격리" vs "전체 실행 중단"의 경계

- **개별 주문 제출 실패**(예: 첫매수 사다리 5건 중 1건만 API 오류)는 **그 주문만 포기하고 나머지는 계속 시도**합니다. 서로 다른 가격대의 독립적인 주문이라 하나가 실패했다고 나머지를 포기할 이유가 없기 때문입니다. 단, 실패한 주문 각각에 대해 CRITICAL 알림이 뜨므로 로그에서 몇 건이 실패했는지 반드시 확인해야 합니다.
- **상태/설정/로직 오류**(`StateError`/`FormulaError`/`NormalModeError`/`ConfigError`)는 재시도할수록 위험해지는 종류라서 **스케줄러 자체를 종료**시킵니다. `docker-compose.yml`의 `restart: unless-stopped`와 조합되므로, 원인이 지속적이면 컨테이너가 재시작될 때마다 같은 오류로 다시 죽고 다시 CRITICAL 알림이 옵니다 — "반복 알림 = 자동 재시작에 맡기지 말고 직접 조치하라"는 신호로 해석하세요.

---

## 5. 부분체결 처리

### 5-1. 판정 로직 (`scheduler._classify_daily_events`)

핵심 공식:

```
fill_ratio = buy_filled / buy_intended
is_full = fill_ratio >= 0.99   →  FULL_BUY
is_full = fill_ratio <  0.99   →  HALF_BUY
```

- `buy_intended`: **그날 제출된 매수 라운드의 모든 주문**(체결 여부와 무관, 형제 주문 포함)의 `가격 × 수량` 합.
- `buy_filled`: 그중 **실제로 체결된 부분**의 `체결가 × 체결수량` 합.

> **2026-08-03 수정된 버그**: 이전 버전은 `buy_intended`를 "체결된 주문들만" 기준으로 계산해서, 예를 들어 전반전 절반매수(별지점 주문 + 평단 주문) 중 한쪽만 체결되면 그 한쪽 기준으로 100%로 오판(FULL_BUY)하는 문제가 있었습니다. 지금은 그날 제출된 모든 형제 주문(미체결 포함)을 먼저 집계한 뒤 비율을 계산하므로, 절반만 체결되면 정확히 HALF_BUY로 판정됩니다. `tests/test_scheduler.py::test_classify_half_buy_when_only_one_of_two_orders_fills`로 고정 검증됨.

### 5-2. 판정 시나리오 표

| 시나리오 | buy_intended | buy_filled | fill_ratio | 결과 |
|---|---|---|---|---|
| 전반전 절반매수 2건 모두 전액 체결 | 500+480=980 | 980 | 100% | `FULL_BUY` |
| 절반매수 중 1건만 전액 체결, 나머지 미체결 | 980 | 500 | 51% | `HALF_BUY` |
| 단일 주문 10주 중 4주만 부분체결 | 500 | 200 | 40% | `HALF_BUY` |
| 단일 주문 100주 중 99주 체결(반올림 허용) | 5000 | 4950 | 99% | `FULL_BUY` (임계값 이상) |
| 매수 미체결 (0주) | >0 | 0 | 0% | 이벤트 생성 안 함 (`buy_filled > 0` 조건 불충족) |

### 5-3. 왜 이분법(FULL/HALF)만 쓰는가

설계도 2번이 T값 갱신 규칙을 "전액/절반" 두 케이스로만 정의하고 있어, 실제로는 60%·80% 같은 애매한 부분체결도 임계값 0.99를 기준으로 강제로 둘 중 하나에 반올림합니다. 이 임계값(`_FULL_FILL_RATIO_THRESHOLD`)은 `scheduler.py` 상단 상수로 되어 있어 필요하면 조정 가능하지만, TQQQ처럼 유동성이 큰 종목의 LOC 주문은 실제로는 대부분 100% 또는 0%로 체결되고 애매한 중간값은 드뭅니다.

---

## 6. 중복 실행 / 멱등성

### 6-1. 판정 키

**`(run_type, run_date)`** — `daily_run_log` 테이블의 PRIMARY KEY.

- `run_type`: `"PREMARKET"` 또는 `"REGULAR"` (두 진입점이 독립적으로 각자 선점)
- `run_date`: America/New_York 기준 오늘 날짜(`YYYY-MM-DD`)

`run_premarket_update()`/`run_regular_session_orders()`는 각각 실행 맨 앞(단, 시간대 체크처럼 side-effect 없는 조건 이후)에서 `_claim_daily_run(conn, run_type, today)`을 호출해 이 키를 `INSERT`합니다. SQLite의 PRIMARY KEY 제약이 원자적으로 "먼저 온 하나만 통과"를 보장하므로, 프로세스가 정확히 같은 순간에 두 번 뜨는 경쟁 상황에도 안전합니다.

### 6-2. `submitted_orders`와의 역할 분리 (중요)

두 테이블은 목적이 다릅니다 — 혼동하기 쉬운 지점이라 명시합니다:

| | `daily_run_log` | `submitted_orders` |
|---|---|---|
| 막는 대상 | "오늘 이 작업을 **다시 실행**하는 것" 자체 | 체결이 들어왔을 때 "이게 **무슨 주문**이었는지" 모르는 것 |
| 언제 쓰나 | 함수 진입 직후, 아무 주문도 내기 전에 | 주문을 실제로 제출한 **이후**, 다음날 체결과 대조할 때 |
| 없으면 생기는 문제 | 컨테이너 재시작 시 오늘 주문이 두 번 나감(중복 매수/매도) | 체결이 들어와도 그게 별지점 매수였는지 평단 매수였는지 몰라 T값을 못 갱신함 |

즉 `daily_run_log`가 **"오늘 이미 주문을 냈는가"**를 막고, `submitted_orders`는 **그렇게 낸 주문의 정체를 기억**합니다. 둘 다 있어야 "중복 제출 방지"와 "체결 종류 판정"이 각각 성립합니다.

### 6-3. 왜 "실패해도 재시도 안 함"인가

`_claim_daily_run`은 **작업 시작 직전에 먼저 선점**합니다. 즉 이번 실행이 선점 이후 중간에 죽더라도(주문 3건 중 1건만 내고 프로세스 다운) `daily_run_log` 행은 이미 남아있으므로, **재시작해도 같은 날 같은 작업이 자동으로 다시 실행되지 않습니다**. "실패하면 안전하게 재시도"가 아니라 "실패하면 사람이 확인하기 전까지 멈춰있는 것"을 택한 이유는, 재시도가 주문 중복 제출로 이어질 위험이 부분 실행 상태를 방치하는 위험보다 크다고 판단했기 때문입니다. 실제 재실행 절차는 8-1 참고.

---

## 7. 드라이런 모드

### 7-1. 왜 `DRY_RUN`이 유일한 사전 검증 수단인가

키움증권 모의투자는 해외주식(미국주식)을 지원하지 않습니다. 그래서 이 프로젝트에는
"모의투자 계좌로 먼저 체결까지 검증하고 실투자로 전환한다"는 일반적인 경로가 아예
없습니다 — 처음부터 끝까지 실투자 API만 존재합니다. `DRY_RUN`은 그 대체 수단으로,
API 호출 자체는 항상 실계좌를 대상으로 하되(그래야 실제 시세/체결 데이터로 로직을
검증할 수 있으므로) 실제 주문 제출 한 단계만 건너뜁니다.

| `DRY_RUN` | 시세/체결 조회 | 실제 주문 제출 | 용도 |
|---|---|---|---|
| `true` (권장 초기값) | O (실계좌 데이터) | X | 실계좌 시세로 오늘 어떤 주문이 나갈지 계산만 하고 로그로 확인. 실주문 전 필수 단계 |
| `false` | O | O (실주문) | 실운영. 계좌에 실제로 자금이 오가므로, `true`로 충분히 검증한 뒤에만 전환 |

`DRY_RUN=true`일 때도 시세 조회(`get_quote`, `get_recent_daily_closes`)와 체결 조회는 정상 호출됩니다 — 계산 로직을 실데이터로 검증하기 위함입니다. 오직 `submit_order_with_retry()` 호출(=실제 주문 제출)만 건너뜁니다. 모든 주문 제출은 `scheduler._submit_or_dry_run()` 한 곳을 반드시 거치도록 되어 있어, 이 플래그가 새어나가지 않습니다.

### 7-2. 사용 예

```bash
# .env에서
DRY_RUN=true

# 실행 후 로그 확인 (실제 API 호출 없이 계산 결과만 남음)
docker compose logs -f
# [DRY-RUN] 주문 미제출 (실제 API 호출 없음): side=BUY kind=LOC price=48.50 qty=12 purpose=HALF_STAR decoy=False
```

`daily_run_log`/`submitted_orders`와의 상호작용: 드라이런 모드에서는 `submitted_orders`에 아무것도 기록하지 않습니다(대조할 실제 주문이 없으므로). 반면 `daily_run_log` 선점은 드라이런 여부와 무관하게 정상적으로 이루어집니다(같은 날 같은 작업을 반복 실행하지 않는다는 멱등성 원칙은 드라이런에서도 유지).

---

## 8. 장애 복구 런북

> 공통 준비물: 컨테이너 안에서 명령을 실행하려면 `docker compose exec infinite_buying bash`로 먼저 들어가거나, `docker compose run --rm infinite_buying <명령>` 형태로 실행합니다. 아래 명령은 프로젝트 루트에서 로컬 실행(`python -m ...`) 기준으로 적었습니다.

### 8-1. 프로세스가 장중에 중단됐을 때

**증상**: 컨테이너가 재시작됐거나(`docker compose ps`에서 `Restarting`/재시작 횟수 확인), 로그에 CRITICAL 알림이 찍혀 있음.

1. 원인부터 확인합니다.
   ```bash
   docker compose logs --tail 200 infinite_buying
   ```
   CRITICAL 로그 메시지에 예외 타입과 위치가 그대로 남아 있습니다(`_run_guarded`가 남긴 로그).

2. **어제/오늘 실제로 주문이 나갔는지** 확인합니다 (재시작으로 인한 중복 제출이 있었는지):
   ```bash
   python -m infinite_buying_v4.verify_replay
   ```
   이 명령 자체는 T값만 검증하지만, 함께 `sqlite3 data/infinite_buying.db "SELECT * FROM daily_run_log ORDER BY claimed_at DESC LIMIT 5;"` 로 오늘 PREMARKET/REGULAR가 이미 선점(=이미 실행 시도)됐는지 확인하세요.

3. **원인이 일시적**(네트워크 순단 등)이라면: 컨테이너가 이미 `restart: unless-stopped`로 재시작되어 있을 것입니다. 다음 스케줄(다음 프리장/본장)까지 기다리면 자동으로 재개됩니다. 단, 오늘 몫의 작업은 6장 정책상 재시도되지 않으므로, **오늘 하루는 건너뛴 것으로 간주**하고 넘어갑니다(내일 정상 진행).

4. **원인이 지속적**(설정 오류, 상태 손상 등)이라면: 컨테이너를 세워두고 원인부터 고칩니다.
   ```bash
   docker compose stop infinite_buying
   ```
   설정 오류면 `.env` 수정, 상태 손상이면 8-3 절차를 먼저 수행합니다.

5. **오늘 몫 작업을 수동으로 강제 재실행**해야 하는 경우(예: 프리장 갱신이 통째로 실패해서 오늘 지정가매도가 안 걸렸음을 사람이 확인함): 정말 필요할 때만, 실제로 아무 주문도 안 나갔음을 먼저 확인한 뒤 선점을 수동 해제합니다.
   ```bash
   sqlite3 data/infinite_buying.db "DELETE FROM daily_run_log WHERE run_type='PREMARKET' AND run_date='2026-08-03';"
   python -m infinite_buying_v4.scheduler   # 또는 run_premarket_update를 직접 호출하는 1회성 스크립트
   ```
   **주의**: 이미 일부 주문이 나간 상태에서 이 절차를 밟으면 중복 제출됩니다. 반드시 `submitted_orders`와 증권사 앱/HTS의 실제 주문 내역을 대조한 뒤 진행하세요.

### 8-2. 주문 제출이 재시도 후에도 실패했을 때

**증상**: `submit_order_with_retry`가 3회 재시도 후 CRITICAL 알림 ("주문 제출 반복 실패").

1. 로그에서 실패한 주문의 `purpose`/`price`/`qty`를 확인합니다(알림 메시지에 그대로 포함됨).
2. 증권사 쪽 문제인지 확인:
   ```bash
   # 계좌 상태/예수금 확인 (auth, 잔고 문제인지 API 자체 장애인지 구분)
   python -c "
   from infinite_buying_v4.config import load_config
   from infinite_buying_v4.kiwoom_adapter import get_usd_deposit
   print(get_usd_deposit(load_config()))
   "
   ```
   - 예수금 조회도 실패 → API/인증 전체 장애. 키움 서버 상태 또는 App Key/Secret 만료 여부 확인.
   - 예수금 조회는 되는데 주문만 실패 → 해당 종목/가격대 특이사항(가격 제한폭 등) 의심. 수동으로 HTS에서 같은 조건 주문을 시도해 원인 파악.
3. 문제가 해소되면, 실패했던 그 주문만 수동으로 다시 낼지(HTS 직접 주문) 판단합니다. **스케줄러를 재실행해도 `daily_run_log`가 이미 선점되어 있으면 나머지 주문이 자동으로 다시 나가지 않습니다** — 6장 정책. 그날의 나머지 미제출 주문은 8-1의 5번 절차(선점 수동 해제)로만 재시도 가능하며, 이 경우 이미 성공한 주문까지 다시 나가지 않도록 각별히 주의해야 합니다(현재 구현은 "라운드 전체 재생성" 방식이라 개별 주문 단위 재시도 기능은 없습니다).

### 8-3. T값이 실제 잔고와 안 맞을 때

**증상**: 대시보드/HTS 확인 결과 보유수량·평단은 맞는데 오늘 걸린 별지점 가격이 이상하다, 또는 `state.t`가 예상과 다르다.

```bash
# 1) 읽기 전용 검증 — 무엇이 다른지, 이벤트별 T 변화 추적(trace)까지 출력
python -m infinite_buying_v4.verify_replay
# 종료코드 0 = 일치(문제 없음), 1 = 불일치, 2 = 실행 자체 실패(설정/DB/JSONL 오류)

# 2) 불일치라면, 무엇으로 고칠지 미리보기 (아직 아무것도 쓰지 않음)
python -m infinite_buying_v4.verify_replay --repair

# 3) 미리보기 값이 맞다고 확신하면 실제 반영
python -m infinite_buying_v4.verify_replay --repair --apply

# 4) 재검증으로 확정
python -m infinite_buying_v4.verify_replay
```

- 이 도구는 **현재 사이클**(`state.cycle_start_date` 이후)의 이벤트만 재생합니다. 출력되는 표(날짜 / 이벤트 / T 이전→이후)를 보고, 특정 날짜의 이벤트 분류(`FULL_BUY`/`HALF_BUY`/...)가 실제 그날 체결 상황과 맞는지 사람이 대조하세요 — 자동 재생 자체가 틀린 이벤트를 다시 재생하면 잘못된 값으로 "일치"해버릴 수 있습니다(이벤트 로그 자체가 틀렸다면 5장 판정 로직까지 같이 재검토).
- `--repair --apply`는 **`state.t`만** 고칩니다. `avg_price`/`holding_qty`/`remaining_cash`가 함께 틀어진 상태라면 별도로 `sqlite3`로 직접 확인/수정해야 합니다(자동화되어 있지 않음 — 잘못 건드리면 더 위험한 값들이라 의도적으로 수동 검증을 요구합니다).

### 8-4. `event_log.jsonl`이 손상됐을 때

**증상**: `verify_replay.py` 실행 시 `[오류] 이벤트 로그 읽기 실패(파일 손상 의심)` 메시지와 함께 종료코드 2.

1. 먼저 원본을 보존합니다(실수로 더 망가뜨리지 않기 위해).
   ```bash
   cp data/event_log.jsonl data/event_log.jsonl.broken.$(date +%Y%m%d%H%M%S)
   ```
2. 손상 위치를 찾습니다 — 에러 메시지에 몇 번째 줄인지 나옵니다(`EventLogError`가 `line_no` 포함).
   ```bash
   sed -n '<N-2>,<N+2>p' data/event_log.jsonl.broken.*   # 손상 줄 앞뒤 확인
   ```
   흔한 원인: 디스크 풀로 인한 마지막 줄 쓰다 만 것(대부분 마지막 줄), 수동 편집 중 JSON 문법 깨짐.
3. **마지막 줄이 깨진 경우** (가장 흔함): 그 한 줄만 제거합니다.
   ```bash
   # 총 줄 수 확인 후, 마지막 줄 제거
   sed -i '$d' data/event_log.jsonl
   ```
4. 다시 검증합니다.
   ```bash
   python -m infinite_buying_v4.verify_replay
   ```
   - 이제 파일이 읽히고 T값도 일치하면 → 그 마지막 줄에 해당하는 이벤트 하나가 유실된 것입니다. 그 이벤트가 무엇이었는지(어제 체결 내역과 대조) 파악해서 `event_log.append_event()`로 수동으로 다시 추가하거나, 8-3 절차로 T값을 강제 보정합니다.
   - 여전히 실패하면 → 중간 줄이 손상된 것입니다. 3번 절차를 손상 줄에 대해 반복하되, **중간 줄을 통째로 삭제하면 그 이후 모든 재생이 달라지므로**, 삭제 전 해당 줄의 `event_type`을 최대한 복구해서 다시 써넣는 것을 우선 시도하세요(JSON 문법만 깨지고 값 자체는 읽을 수 있는 경우가 많음).
5. 복구 후 반드시 `--repair --apply`(8-3)로 `state.t`를 재생 결과에 맞춰 재동기화합니다.

---

## 9. 빠른 참조 (한 페이지 요약)

```
장애 종류              진단 명령                                              조치
─────────────────────────────────────────────────────────────────────────────
프로세스 중단           docker compose logs --tail 200                        8-1
주문 제출 실패          (알림 로그의 purpose/price/qty 확인) + 예수금 조회 스크립트   8-2
T값 불일치             python -m infinite_buying_v4.verify_replay             8-3
JSONL 손상             python -m infinite_buying_v4.verify_replay (exit 2)    8-4
중복 실행 의심          sqlite3 ... "SELECT * FROM daily_run_log"              6장
오늘 뭐가 제출됐는지     sqlite3 ... "SELECT * FROM submitted_orders"           3-1
```
